name: Test

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
      - uses: ./
        with:
          typst_ref: 5b0297464efc131beb7be84fa7a02b9a8670b5dd
          retention_days: 7
          artifacts_name: typst_output
          output_extensions: pdf
